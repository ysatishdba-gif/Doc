resp = self.session.post(self.url, headers=self.headers,
    json={"query_texts": ["left knee pain"], "top_k": 3}, timeout=200)
data = resp.json()
# print first key and first 3 items
for key, val in data.items():
    if isinstance(val, list):
        print(f"Key: {key}")
        for item in val[:3]:
            print(f"  {item}")
        break
