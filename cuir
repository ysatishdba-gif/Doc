# %% Check: does your API return scores per CUI?
resp = extractor.session.post(
    extractor.url, headers=extractor.headers,
    json={"query_texts": [text], "top_k": 3},
    timeout=200)
data = resp.json()

# show first 5 items from each key
for key, val in data.items():
    if isinstance(val, list):
        print(f"\n{key} (first 5):")
        for item in val[:5]:
            print(f"  {item}")
