# %% See the impact of zero-overlap-only blocking
# This works for ANY text because it's structural
zero_overlap_blocked = (ratios == 0).sum()
would_still_collapse = (ratios > 0).sum()

print(f"Current terms_diverge blocks: {len(ratios)}")
print(f"Zero-overlap only would block: {zero_overlap_blocked}")
print(f"Would unblock (let embeddings decide): {would_still_collapse}")
print(f"Reduction improvement: ~{would_still_collapse} more pairs eligible")
