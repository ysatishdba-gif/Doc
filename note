Pass 1 (three steps):

  Step 1: gap_split on centroid similarities → admits some groups     ← you're here
  
  Step 2: CUI overlap rescue
    For every EXCLUDED group, check: does it share any CUI with the query?
    group_71 shares C3873937 (Knee femur prosthesis) with query → admitted
    (centroid was low because the topic is mostly about something else,
     but it contains one exact query CUI — worth scoring)
  
  Step 3: fallback
    If zero groups passed Step 1 AND Step 2 → score ALL 236 groups in Pass 2
    (better to score everything slowly than miss the right answer)

Pass 2: score each entry in admitted groups (three signals: cui_ratio, vec_sim, lin_avg)

Pass 3: z-score normalize across all scored entries

Pass 4: keep everything ≥ 40% of best score → final results
