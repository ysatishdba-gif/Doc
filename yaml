# Production CUI Reduction Configuration
# All values must be explicitly defined - no defaults

# BigQuery Configuration
project_id: "your-gcp-project"
dataset_id: "your-bigquery-dataset"

# Table Names (must exist with proper schema)
mrrel_table: "MRREL"
mrconso_table: "MRCONSO" 
cui_embeddings_table: "cui_embeddings"
cui_descriptions_table: "cui_descriptions"

# API Configuration
api_url: "https://your-cui-api.run.app/extract"
api_key: "${CUI_API_KEY}"  # Use environment variable for sensitive data

# Terminology Systems
allowed_sab:
  - "ICD10"
  - "ICD10CM"
  - "ICD10PCS"
  - "ICD9CM"
  - "SNOMEDCT_US"
  - "LOINC"

# Reduction Parameters
target_reduction: 0.85
ic_percentile: 50.0
semantic_threshold: 0.88
duplicate_threshold: 0.95
max_hierarchy_depth: 3
min_cluster_retention: 0.3

# Performance Settings
batch_size: 1000
query_timeout: 300
max_query_results: 100000
cache_enabled: true

# Observability
enable_metrics: true
enable_cloud_logging: true
metrics_port: 8000
